#include<stdio.h>
int main()
{
    int t1=0,t2=1,nt,n;
    nt=t1+t2;
    printf("ENter n term:");
    scanf("%d",&n);
    printf("Fibbonacci series: %d %d",t1,t2);
    do
    {
        printf(" %d ",nt);
        t1=t2;
        t2=nt;
        nt=t1+t2;
        
    } while (nt<=n);
    return 0;
}
